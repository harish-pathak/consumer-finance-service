# Consumer Finance Service - Docker Environment Configuration
# Copy this file to .env and update the values as needed

# ==================== MySQL Configuration ====================
MYSQL_ROOT_PASSWORD=rootpassword123
MYSQL_DATABASE=consumer_finance
MYSQL_USER=financeuser
MYSQL_PASSWORD=financepass123
MYSQL_PORT=3307

# ==================== Application Configuration ====================
APP_PORT=8080
SPRING_PROFILES_ACTIVE=docker
LOG_LEVEL=INFO
APP_LOG_LEVEL=DEBUG

# ==================== Security Configuration ====================
# IMPORTANT: Generate a secure 32-byte Base64-encoded key for production
# You can generate one using: openssl rand -base64 32
ENCRYPTION_KEY=defaultEncryptionKey1234567890123456

# ==================== OAuth2 Configuration ====================
# Configure your Auth0 or other OAuth2 provider details
# Leave as default for testing without OAuth2 (will return 401 for protected endpoints)
OAUTH2_ISSUER_URI=https://dev-dummy.auth0.com/
OAUTH2_JWK_SET_URI=https://dev-dummy.auth0.com/.well-known/jwks.json

# ==================== Notes ====================
# - Copy this file to .env before running docker-compose
# - Update passwords and keys for production use
# - The database will be automatically initialized with:
#   * Schema creation via Flyway migrations
#   * Sample vendor data
# - Access the application at: http://localhost:8080
# - Health check: http://localhost:8080/api/v1/health
# - API documentation: http://localhost:8080/swagger-ui.html
